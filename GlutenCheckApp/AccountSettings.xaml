<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GlutenCheckApp.AccountSettings"
             Title="AccountSettings">
    <Grid Padding="25" RowSpacing="15" ColumnSpacing="15" BackgroundColor="Wheat">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label
    Grid.Row="0"
    Grid.ColumnSpan="2"
    FontSize="16"
    TextColor="Black"
    HorizontalOptions="Start"
    Text="{Binding LoggedInText, TargetNullValue='Inte inloggad'}"/>
        <Label
Grid.Row="1"
Grid.ColumnSpan="2"
FontSize="16"
TextColor="Black"
HorizontalOptions="Start"
Text="Dina Allergener"/>

        <CollectionView 
            ItemsSource="{Binding AllAllergens}"
                        
            Grid.Row="2"
                        
            Grid.Column="0"
            
            
            HorizontalOptions="Fill"
                        >
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="*,Auto" Padding="5">

                        <Label 
                            Text="{Binding Name}" 
                
                            FontSize="18"
                            
                            VerticalOptions="Center"
                
                            TextColor="Black" />
                        <Switch
                            Grid.Column="1"
                            IsToggled="{Binding IsSelected}"
                            />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button
            Grid.Row="4"
            Grid.Column="1"
            
            Text="Spara"
            
            Command="{Binding SaveUserAllergensCommand}" />
    </Grid>
</ContentPage>